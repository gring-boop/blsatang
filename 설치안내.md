# 벨사탕 작업실 — 이번 업데이트 안내

더마감에 만든 기능을 벨사탕에도 넣었습니다. **두 방은 서버가 완전히 달라서 데이터는 절대 섞이지 않습니다.**
같은 기능을 각자 따로 가진 것뿐이에요.

올리기 전에 **파이어베이스에서 두 가지**를 하셔야 합니다. 안 하면 아무것도 저장되지 않아요.

---

## ① 로그인 켜기 ← 안 하면 아무도 못 들어옵니다

이제 필명과 **비밀번호**로 들어옵니다.

1. <https://console.firebase.google.com> → **벨사탕 프로젝트** 선택
   (더마감 프로젝트가 아닙니다! 위쪽 프로젝트 이름을 꼭 확인하세요)
2. 왼쪽 메뉴 **보안 → Authentication** (예전 메뉴에서는 빌드 → Authentication)
3. **시작하기** → **이메일/비밀번호** → 맨 위 **사용 설정** 켜기 → **저장**

### 이메일을 안 받는데 왜 이메일 로그인인가요

파이어베이스가 이메일을 요구해서, **필명을 가짜 주소로 바꿔서** 씁니다.

    콩  →  n5f435b@belsatang.local

메일이 오가지 않는 주소예요. Authentication 목록에 이상한 주소가 쌓이는 게 정상입니다.
**더마감은 `@themagam.local`, 벨사탕은 `@belsatang.local`** 로 서로 다릅니다.

### 비밀번호를 잊은 사람이 생기면

메일이 진짜가 아니라 재설정 메일을 못 보냅니다. 방장이 직접 처리해야 해요.

- **Authentication → Users** 에서 그 계정을 **삭제**
- **Realtime Database → nickOwner → 그 필명** 도 **같이 삭제**

둘 다 지워야 그 사람이 새 비밀번호로 다시 만들 수 있습니다. 채팅·기록·펫은 그대로 남습니다.

---

## ② 보안 규칙 게시 ← 안 하면 저장이 조용히 거절됩니다

**Realtime Database → 규칙** 탭에서 칸을 전부 지우고, 함께 드린 **`보안규칙.json`** 을 통째로 붙여넣고 **게시**.

이 규칙이 하는 일입니다.

- 로그인한 사람만 쓸 수 있게 합니다
- `users/{필명}`, `status/{필명}`, 글자수 기록은 **그 필명의 주인만** 쓸 수 있습니다
- 필명 도장(`nickOwner`)은 한 번 찍히면 **아무도 바꿀 수 없습니다**

> 지금 규칙이 "테스트 모드"라면 30일 뒤 잠깁니다. 이번에 꼭 바꿔주세요.

---

## ③ 깃허브에 올리기

폴더 안 파일을 **전부** 올리시면 됩니다 (`icons` 폴더 포함).

올리지 않아도 되는 파일: `build-single.py`, `checks.js`, `설치안내.md`, `보안규칙.json`, `index-단일파일.html`, `UI리뉴얼_리뷰.md`

---

## 무엇이 달라졌나

**프로필 카드** — 더마감과 같은 모양입니다.

- **프사를 누르면** 프로필 설정 (사진·색·무늬)
- **상태표를 누르면** 상태 바꾸기
- **카드 아래를 누르면** 내 카드는 목표·투두, 남의 카드는 기록 보기
- 업적 배지(🏆👑)는 **닉네임 앞**에 작게 붙습니다

**펫 키우기** — WORK 와 집중 시간만 밥이 됩니다. 5시간에 1레벨, 100시간에 만렙(Lv.20).
만렙을 찍으면 도감에 들어가고 다음 펫이 저절로 시작돼요. 20종이 전부 다른 종류로 나옵니다.

**뽀모** — "대기 중" 알약과 진행 바를 하나로 합쳤습니다. 알약 안쪽이 차오르면서 `🍅 작업 · 23분 30초` 처럼 보입니다.

**글자수** — 지금 원고의 전체 글자수를 적으면 직전과의 차이만 쌓입니다. 오늘 탭은 채팅처럼 흐르고, 내 기록 탭에서 이번 주 요일별 그래프를 봅니다.
순위는 어디에도 나오지 않습니다 — 그날 적게 쓴 사람이 위축되지 않게요.

**창이 넷으로** — 채팅 · 접속자 · 뽀모 · 글자수.
오늘 목표와 나의 투두는 창에서 빠지고 **카드 아래 팝업 안**으로 들어갔습니다. **저장된 내용은 그대로 남습니다.**
칸 수가 바뀌어서 **배치 설정은 한 번 초기화**됩니다 (크기와 순서만, 기록은 안 날아갑니다).

세로 보기는 이제 네 덩이를 위아래로 쌓습니다. 전에 "세로 보기가 세로로 안 보인다" 하셨던 것이 이걸로 해결됐어요.

---

## 확인할 것

- 필명 + 비밀번호로 입장이 되는가
- 같은 필명에 **틀린 비밀번호**로 들어가면 막히는가
- 프사·상태표·카드 아래를 눌렀을 때 각각 다른 창이 뜨는가
- 설정 → 🐾 펫 에서 펫이 보이는가
- 글자수를 적으면 채팅처럼 올라가는가
- 뽀모를 시작하면 알약이 차오르는가

더마감에 들어가 봐서 **벨사탕 접속자가 안 보이면** 분리가 잘 된 것입니다.
