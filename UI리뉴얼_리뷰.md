# 벨사탕 작업실 — UI/UX 리뉴얼 리뷰

**대상** BL-main (7,156줄 · HTML 1 · CSS 1 · JS 6)
**작업 범위** UI/UX 리뉴얼 (`styles.css`, `index.html` 교체)
**날짜** 2026-07-27

---

## 0. 먼저 — 코드에 대한 정직한 평가

이건 잘 만든 물건입니다. 빌드 도구 없이 순수 HTML/CSS/JS로 실시간 채팅, 공유 뽀모도로, 프레즌스, 멘션 자동완성, 답장 인용, 출석 업적까지 굴러가고 있어요. 특히 잘한 부분:

- **프레즌스 처리가 꼼꼼합니다.** `onDisconnect` + `beforeunload` + `pagehide` + bfcache `pageshow` 복귀까지 대응했고, `sendBeacon`이 크로스오리진에서 `application/json`을 막는 것까지 알고 `text/plain`으로 우회했습니다. 이건 실제로 당해봐야 아는 문제입니다.
- **XSS 방어가 되어 있습니다.** `escapeHtml`을 채팅 렌더에 27곳 적용했고, 투두처럼 사용자 입력이 들어가는 곳은 `innerHTML`로 뼈대만 만들고 텍스트는 `textContent`로 따로 넣었습니다. 올바른 패턴입니다.
- **CSS 변수 기반 테마 시스템**이 이미 자리 잡혀 있어서, 이번 리뉴얼이 JS를 한 줄도 안 건드리고 가능했습니다.

코드 곳곳의 `✅ [FIX]` 주석들이 실제 버그를 하나씩 잡아온 기록이라, 리뉴얼하면서 그 의도를 최대한 보존했습니다.

---

## 1. ⚠️ UI 범위 밖이지만 먼저 알려야 하는 것

리뉴얼과 별개로, 코드를 읽다 발견한 사항입니다. 판단은 두 분이 하시면 됩니다.

### 1-1. 관리자 PIN이 클라이언트 코드에 평문으로 있습니다 — 우선순위 높음

```js
// script_realtime.js:612
if (p === "59595959") {
  sessionStorage.setItem("adminPinOk", "true");
```

브라우저에서 F12 → Sources를 열면 누구나 보입니다. 그리고 관리자 권한에는 **채팅 전체 삭제(`clearAllChat`)** 가 포함되어 있습니다.

더 중요한 건 PIN 유출 자체가 아니라 **검증이 클라이언트에만 있다는 점**입니다. PIN을 몰라도 콘솔에 아래 한 줄이면 관리자가 됩니다.

```js
sessionStorage.setItem("adminPinOk", "true")
```

즉 PIN을 바꾸는 것만으로는 해결되지 않습니다.

**어떻게 고치나** — 코드 전체 구조를 바꾸지 않고도 가능한 순서대로:

1. **(최소)** Firebase Realtime DB **보안 규칙**에서 `messages` 전체 삭제와 `chatMeta` 쓰기를 막습니다. UI에 버튼이 보이든 말든 서버가 거부하면 실제 피해는 안 납니다. 지금 코드에 `firebase.auth()` 호출이 한 곳도 없으므로, 규칙이 `".write": true` 상태일 가능성이 높습니다 — **콘솔에서 규칙부터 확인해 보시길 권합니다.**
2. **(권장)** Firebase Anonymous Auth를 켜고, 관리자 UID를 규칙에 하드코딩합니다. 로그인 UI 없이 `signInAnonymously()` 한 줄로 되고, 규칙에서 `auth.uid`로 판별할 수 있습니다.
3. 그 다음에 PIN 상수를 코드에서 지웁니다.

### 1-2. 필명이 DB 키입니다

`status/{필명}`, `users/{필명}/prefs` 구조라 동명이인이 들어오면 서로의 테마·투두·목표를 덮어씁니다. 설정 모달에 "중복되지 않는 필명을 권장해요"라고 적어두신 걸 보면 이미 아시는 부분 같습니다. 근본 해결은 익명 UID를 키로 쓰고 필명은 표시용 필드로 내리는 것인데, 기존 데이터 마이그레이션이 필요해서 이번 범위에는 넣지 않았습니다.

### 1-3. 죽은 참조 2건 (무해하지만 정리 권장)

`db-today-done`, `today-progress-text` — JS 9곳에서 참조하는데 HTML에 요소가 없습니다. "오늘 누적" 필드를 걷어내면서 JS 정리가 덜 된 흔적으로 보입니다. 전부 `?.`로 감싸져 있어 에러는 안 나지만, `savePersonalData`가 매번 `todayDone: ""`를 DB에 쓰고 있습니다.

---

## 2. UI/UX 진단 — 무엇이 문제였나

### 2-1. 다크 테마에서 안 보이는 텍스트 (가장 실질적인 버그)

`.hint`, `.mini-hint`, `.field-label`, `.personal-area::before`가 색을 `rgba(60,60,67,.55)`로 **하드코딩**하고 있었습니다. 이건 어두운 회색인데, 다크 테마 배경도 어두운 회색입니다.

```css
/* 기존 — --muted 토큰이 이미 있는데도 안 씀 */
.hint{ color: rgba(60,60,67,.55); }
```

테마가 56개고 그중 상당수(`밤샘 · 무채 차콜` 계열 등)가 다크입니다. 그 테마들에서 설명 문구가 사실상 읽히지 않았습니다. 하드코딩 색상이 총 35곳 있었습니다 (`rgba(60,60,67,…)` 17, `rgba(120,120,128,…)` 10, `#0A84FF` 8).

**→ 전부 토큰화했습니다.** 특히 `#0A84FF`를 `--on-accent-btn`으로 바꾸면서, 다크 테마에서 파란 글씨가 파란 배경 위에 얹히던 문제도 같이 해결됩니다.

### 2-2. 폰트 웨이트가 전부 굵어서 위계가 없었습니다

`font-weight: 900` 또는 `950`이 **47곳**에 있었습니다. 카드 이름도 950, 힌트도 650, 시간 표시도 900, 툴바 버튼도 950.

문제가 두 겹입니다.

1. **위계 소실** — 전부 굵으면 눈이 어디를 먼저 봐야 할지 모릅니다. 화면에서 가장 중요한 "오늘의 목표"와 부차적인 "12:34" 타임스탬프가 같은 무게로 보였습니다.
2. **950은 존재하지 않는 값입니다** — `-apple-system`, `Pretendard`, `Apple SD Gothic Neo` 어느 것도 950 웨이트를 갖고 있지 않아서 브라우저가 700(또는 800)으로 내려서 렌더합니다. 즉 900과 950이 **화면상 완전히 동일**했습니다. 의도한 차이가 코드에만 있고 화면에는 없었던 셈입니다.

**→ 3단계로 재정의했습니다.** `--fw-normal: 400` / `--fw-medium: 600` / `--fw-bold: 700`. 900+ 사용은 0곳입니다.

| 요소 | 기존 | 리뉴얼 |
|---|---|---|
| 오늘의 목표 / 선언 텍스트 | 950 → 700 렌더 | **700** |
| 카드 이름 / 섹션 제목 | 950 → 700 렌더 | **700** |
| 말풍선 본문 | (미지정) | **400** |
| 버튼 / 라벨 / 필 | 900~950 → 700 렌더 | **600** |
| 타임스탬프 / 힌트 | 650~900 | **400** |

### 2-3. `!important` 51개 — 대부분 증상 치료였습니다

가장 큰 덩어리가 이 블록이었습니다.

```css
/* 기존 — "THEME PATCH OVERRIDES (SAFE)" */
:root[data-is-dark="true"] button{ color: rgba(255,255,255,.92) !important; }
:root[data-is-dark="true"] #timer-wrap .timer-controls button{ ... !important; }
:root[data-is-dark="true"] .ghost-btn.primary{ ... !important; }
/* ... 7개 규칙 */
```

원인은 `.ghost-btn.primary { color: #0A84FF }`가 다크 테마에서 안 보였던 것이고, 해결책이 "다크일 때 강제로 흰색"이었습니다. 하지만 `button` 전체에 `!important`를 걸어버려서, 그 뒤로 어떤 버튼 색도 정상적으로 지정할 수 없는 상태가 됐습니다.

**→ 원인을 고쳤습니다.** `--on-accent-btn` 토큰이 `[data-is-dark="true"]`에서 자동으로 뒤집히므로, 패치 블록 전체가 필요 없어졌습니다.

`.todo-menu`의 `!important` 6개도 같은 성격이었습니다 — 같은 셀렉터가 파일 안에 **두 번** 정의되어 서로를 덮어쓰고 있었고, 나중 것이 이기려고 `!important`를 붙였습니다. 정의를 하나로 합쳐서 해결했습니다.

> `!important` 51 → **20개**. 남은 20개는 `.hidden`처럼 실제로 최우선이어야 하는 것들입니다.

### 2-4. 반응형 브레이크포인트가 980px 하나뿐이었습니다

```css
.main-workspace{ min-width: 520px; }
.chat-sidebar{ min-width: 260px; }  /* 기본 360px */
.container{ padding: 14px; gap: 14px; }
```

520 + 360 + 42 = **922px**. 즉 창 너비가 922~980px 구간이면 세로 스택으로 전환되기 전인데 내용은 이미 안 들어가서, **가로 스크롤바가 생겼습니다.** 13인치 노트북에서 브라우저를 반쪽 크기로 쓰거나 아이패드 가로모드가 딱 이 구간입니다.

**→ 5단계로 세분화**: 1200 / 1024 / 980 / 720 / 420 + 낮은 높이용 `max-height: 560px`.

### 2-5. 접근성

| 항목 | 문제 | 조치 |
|---|---|---|
| `user-scalable=0` | **WCAG 1.4.4 위반.** 저시력 사용자가 핀치줌으로 본문을 못 키웁니다 | 제거. iOS 자동확대는 `#message` 폰트를 16px로 올려 해결 |
| 포커스 표시 | `input`에만 있고 버튼·탭·테마칩엔 없음 → 키보드 사용자가 현재 위치를 알 수 없음 | 모든 인터랙티브 요소에 `:focus-visible` 링 |
| 터치 타깃 | `.pin-remove-btn` 등이 약 20×20px (권장 44×44) | 최소 32~44px로 확대 |
| 무한 애니메이션 | `pomoMegaPulse`, `goldGlow`, `workPillPulse`가 강제 반복 — 전정기관 장애·편두통 유발 요인 | `prefers-reduced-motion` 대응 |
| 라벨 | `<input>` 상당수에 연결된 `<label>` 없음 | `for`/`sr-only` 라벨 추가 |
| 실시간 영역 | 채팅·카드 갱신을 스크린리더가 감지 못함 | `role="log"`, `aria-live` 추가 |
| 고대비 모드 | 미대응 | `prefers-contrast: more` 추가 |

### 2-6. 모바일 100vh 문제

```css
body{ height: 100vh; }
body.narrow-chat-focus .chat-sidebar{ height: calc(100vh - 20px) !important; }
```

iOS Safari / Android Chrome에서 `100vh`는 **주소창이 펼쳐진 상태**의 높이입니다. 스크롤로 주소창이 접히면 실제 뷰포트가 커지는데 요소는 그대로라, **입력창이 화면 밖으로 밀려 잘리는** 고전적인 증상이 납니다. 모바일에서 채팅 전용 모드를 쓰는 앱이라 체감이 컸을 겁니다.

**→ `100dvh`** (동적 뷰포트) + `env(safe-area-inset-bottom)`으로 홈 인디케이터 영역까지 회피.

### 2-7. 자잘한 것들

- **메가 타이머 `font-size: 92px` 고정** — 사이드바를 260px로 줄이면 숫자가 넘쳤습니다. → `clamp(48px, 11vw, 88px)`
- **테마 그리드 `repeat(5, 1fr)` 고정** — 모달이 좁아지면 칩이 찌그러졌습니다. → `auto-fill minmax()`
- **`index.html`에 인라인 `style` 33곳** — 스타일이 두 파일에 흩어져 유지보수 지점이 둘이었습니다. → `.set-row`, `.set-check`, `.stack`, `.w-full` 등 유틸 클래스로 이전
- **죽은 CSS** — `.todo-list > li` 계열 8개 규칙은 예전 "칩 스타일" 투두용인데, JS는 `.todo-item`을 렌더합니다. 삭제했습니다.
- **`.mini-hint` 3중 정의, `.todo-add`·`.todo-plus`·`.todo-input`·`.todo-list` 2중 정의** — 통합했습니다.

---

## 3. 변경 결과

| 지표 | 기존 | 리뉴얼 |
|---|---:|---:|
| `!important` | 51 | **20** |
| 하드코딩 색상 (규칙 내부) | 35곳 | **0곳** (전부 `:root` 토큰) |
| `font-weight: 900+` | 47곳 | **0곳** |
| 반응형 브레이크포인트 | 1단계 | **5단계 + 높이 1** |
| CSS 줄 수 | 1,850 | 2,031 |

줄 수가 늘어난 건 토큰 정의부(약 190줄)와 주석 때문이고, 실제 규칙 수는 줄었습니다.

### 호환성 검증 결과

JS를 한 줄도 수정하지 않아도 되도록, 자동 대조 스크립트로 확인했습니다.

```
1) JS가 찾는 ID 중 새 HTML에 없는 것 ......... 2건 (기존에도 없던 죽은 참조 · §1-3)
2) 유실된 onclick 핸들러 .................... 0건 ✅
3) JS가 setProperty 하는 CSS 변수 32개 ...... 전부 존재 ✅
4) JS가 조작하는 클래스 중 스타일 유실 ....... 0건 ✅
5) CSS 중괄호 균형 / var() 미정의 참조 ....... 정상 / 0건 ✅
```

---

## 4. 적용 방법

`styles.css`와 `index.html` 두 파일만 덮어쓰면 됩니다. JS 6개 파일은 그대로 두세요.

```
BL-main/
├── index.html      ← 교체
├── styles.css      ← 교체
├── fortune_data.js
├── script_core.js      (변경 없음)
├── script_ui.js        (변경 없음)
├── script_chat.js      (변경 없음)
├── script_data.js      (변경 없음)
└── script_realtime.js  (변경 없음)
```

**적용 후 확인해 볼 것**

1. 다크 테마(`밤샘 · 무채 차콜` 등) 선택 → 설정 모달의 회색 힌트 문구가 읽히는지
2. 브라우저 창을 950px 근처로 조절 → 가로 스크롤이 안 생기는지
3. Tab 키로 이동 → 버튼마다 포커스 링이 보이는지
4. 모바일에서 채팅 모드 → 입력창이 하단에 정확히 붙는지
5. 뽀모도로 시작 → 메가 타이머 숫자와 진행바가 정상인지

---

## 5. 이번에 안 한 것 / 다음에 하면 좋을 것

우선순위 순입니다.

1. **DB 보안 규칙 점검** (§1-1) — 유일하게 시급합니다.
2. **테마 56개 → 8~12개로 정리.** 지금 팔레트는 스크롤해야 다 보이고, `밤샘 · 무채 차콜` / `밤샘 · 페이퍼 차콜` / `밤샘 · 스모크 애쉬`는 배경 hex가 `#2A2C2F` / `#2E3033` / `#303336`으로 사실상 구별이 안 됩니다. 선택지가 많은 게 배려처럼 보이지만, 실제로는 고르는 데 드는 비용만 늘어납니다.
3. **테마 정의를 CSS로 이관.** 현재 `script_ui.js`에 테마 객체가 있고 CSS에 토큰이 있어 이중 관리입니다. `[data-theme="..."]` 셀렉터로 CSS에 두면 JS는 속성 하나만 바꾸면 됩니다.
4. **뽀모도로 컨트롤 정리.** 한 줄에 집중분·휴식분·시작·참가중·정지·🎵 여섯 개가 나열되어 있습니다. 시작/정지는 상태에 따라 하나만 보여주면 되고(실행 중엔 시작 버튼이 무의미), 나머지는 🎵 상세 패널로 내려도 무방합니다.
5. **JS 모듈화.** 6개 파일이 전부 전역 스코프이고 `window.*` 전역이 100개입니다. 로드 순서에 의존하고 있어서(`window.xxx?.()` 방어 코드가 그 증거), 파일 순서를 바꾸면 조용히 깨집니다. ESM으로 옮기면 이 위험이 사라집니다.
6. **Firebase compat SDK → modular SDK.** compat은 레거시 호환용이라 번들 전체가 로드됩니다.

---

*질문이나 되돌리고 싶은 부분이 있으면 편하게 말씀 주세요. 기존 파일은 그대로 있으니 언제든 비교·롤백 가능합니다.*
